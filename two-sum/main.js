//Create a function that takes in an array and an interger
//cyle through array
// find two number in array that equal integer
// return location of two numbers in array
function twoSum(arr, sum) {
    let otptionArr = []
    if (sum = isNaN) {
        return ("Please enter a numerical Value" )
    }
    otptionArr.forEach(element => {
        
        
    });
}

module.exports = twoSum;
